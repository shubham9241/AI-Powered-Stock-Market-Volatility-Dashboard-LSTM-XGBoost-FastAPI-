[project]
name = "stock-volatility-dashboard"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "numpy",
    "scikit-learn",
    "xgboost",
    "tensorflow",
    "yfinance",
]

[tool.vercel]
entrypoint = "engine:app"
